# Feature Spec：產線擺設編輯區 - 管線連接
# CR-02

- **所屬階段：** Phase 1（基礎）/ Phase 2（分流器 / 匯流器自動生成）/ Phase 3（自動路徑規劃）
- **依賴：** CR-01（設備擺放狀態與接口位置，管線需依附其上）、CR-03（接口 type 不符 / 佈線違法等警示回饋）、CR-04（物件資訊面板顯示的即時流量估算，管線異動觸發重新估算）、CR-08（管線操作納入操作歷史）
- **文件版本：** v0.4

---

## 1. 功能定位

管線連接模組負責在設備之間建立傳送帶與水管的連線關係，是產線邏輯成立的必要條件。管線的連接狀態是 CR-03（警示）、CR-04（流量估算）的計算基礎。

---

## 2. 功能詳細描述

### 2.1 管線模式切換

- 透過工具列控項或快捷鍵（預設 `P`）切換進入 / 離開管線模式
- 進入管線模式後：
  - 所有設備的接口（Port）自動高亮顯示
  - 所有現有管線自動高亮顯示
  - 游標樣式切換為管線工具樣式

### 2.2 管線 Type 自動判斷

每個設備接口在 `devices.ts` 中定義 `type`（`conveyor` 或 `pipe`）。拉管線時，系統依**起點接口的 type** 決定本次拉的是傳送帶或水管，視覺樣式與連接規則對應切換。不同 type 的接口無法互連（觸發 Error，見 CR-03）。

### 2.3 管線繪製流程（Phase 1：手動彎折點）

**新部署管線：**
1. 在管線模式下，點選設備接口作為起點
2. 移動滑鼠，畫布顯示管線預覽路徑
3. 點選中途空格可新增彎折點（手動決定路徑走向）
4. 移動至目標接口，自動吸附
5. 點選目標接口確認放置；或按 Escape 取消

**已放置管線：**
- Phase 1 不支援編輯已放置管線的彎折點
- 若需調整路徑，須刪除管線後重新建構

**彎折點 90 度限制：**
- 所有相鄰節點（起點、彎折點、終點）之間的線段必須為純水平或純垂直
- 若當前彎折點位置導致任一線段出現斜線，該彎折點顯示紅色警示，且**無法確認放置**，直到所有線段均為 90 度轉角為止
- 系統在預覽時即時偵測並標示違規線段

**繪製中路徑的即時性：**
- 使用者在繪製過程中，應隨時能清楚分辨「已經確定的路徑」與「仍隨游標移動、尚未定案的部分」
- 每點選一個彎折點，起點至該彎折點之間的路徑即視為已確定，樣式與正常管線一致，不再隨後續游標移動而改變
- 從最後一個已確定端點到目前游標位置的這段，樣式需與已確定路徑有明顯區別，讓使用者一眼看出「這段還沒定案」
- 這段跟隨游標的路徑須隨滑鼠移動即時更新，讓使用者在按下確認前，就能持續掌握管線最終大致會怎麼走

> **Phase 3 升級：** 改為自動路徑規劃，系統依當前空間自動生成合法彎折點，並支援放置後調整彎折點。

### 2.4 自動吸附

- 管線端點接近接口時，自動吸附至最近的合法接口
- 吸附範圍：接口中心點周圍 1 格以內
- 吸附時顯示高亮確認提示

### 2.5 移動設備時的自動連接

- 設備編輯行為（見 CR-01：移動 / 旋轉 / 擺放）造成該設備的接口與畫布上既有管線端點對齊時，系統自動建立連接，無需進入管線模式
- 自動連接僅在接口 type 與管線 type 相符時成立；不符合則不連接（維持 2.2 的 type 限制）

### 2.6 特殊節點生成

#### 物流橋（Bridge）
**觸發：** 管線路徑規劃時，判斷路徑必須跨越現有管線
**行為：** 強制自動插入物流橋，不提供切換選項，兩條管線立體交叉互不干擾

> Phase 1 僅物流橋在管線繪製時自動處理，無需使用者手動放置設備。

#### 分流器（Splitter）／匯流器（Merger）— Phase 2

分流器與匯流器本身作為設備，於 Phase 1 即存在於設備庫與工具列（見 `01_canvas_and_devices.md`），使用者可如一般設備手動擺放並自行連接管線；以下「拉管線時自動生成」的互動屬 **Phase 2** 功能：

**分流器觸發：** 從現有管線的中途點拉出新管線
**預設行為：** 自動插入分流器，原管線在該點一分為二，新管線從分流器接出
**使用者可切換：** 點選預示生成的分流器圖示，切換為「截斷模式」
- 截斷模式：原管線在該中途點截斷，截斷點之後的路徑繼續保留，新管線從截斷點接出取代原起點

**匯流器觸發：** 將管線的終點拉至現有管線的中途點
**預設行為：** 自動插入匯流器，兩條管線在該點合流後繼續
**使用者可切換：** 點選預示生成的匯流器圖示，切換為「截斷模式」
- 截斷模式：原管線在該中途點截斷，截斷點之前的起源路徑被捨棄，換由新拉的管線接入後繼續往下

> 所有自動生成的設備均計入設備清單，可被選取、刪除，刪除時管線恢復相應原狀並重新驗證。

### 2.7 管線選取、框選與資訊面板

- 點選單一管線後，顯示物件資訊面板，內容包含管線 type、連接的兩端設備與接口、當前流量估算（見 CR-04）與警示狀態（見 CR-03）
- 管線可跟隨設備一起被框選（見 CR-01 框選機制），框選範圍內的管線與設備一併進行後續編輯行為

### 2.8 管線編輯行為：移動、複製、刪除

- **移動**：整條管線可與其彎折點一併平移，僅平移管線本體，允許脫離原本連接的兩端設備；仍須維持 90 度轉角限制；移動後若端點不再對齊原接口，管線兩端視為未連接，需重新吸附至合法接口才能恢復連接
- **複製**：複製管線時，其彎折點與自動生成節點（分流器 / 匯流器 / 物流橋）一併複製；複製後的管線預設不連接至任何接口，需使用者手動吸附
- **刪除**：點選管線後按 Delete 刪除
- 所有管線操作（新增、移動、複製、刪除、修改彎折點）均可無限復原 / 取消復原（Ctrl+Z / Ctrl+Y，詳見 CR-08）
- 移動導致管線脫離原接口、或刪除後，均自動觸發 CR-04 流量重新估算
- 警示重新計算以**設備接口狀態**為準：管線移動脫離接口或刪除本身不直接產生警示，而是觸發對受影響設備的重新評估（例如某接口因此失去輸入來源，才會產生 E004 輸入缺失）；若管線兩端接口原本就未連接任何有效路徑，則移動或刪除後不產生新警示

---

## 3. 路徑規劃規則（Phase 1）

Phase 1 路徑由使用者透過點選彎折點決定，系統負責：

- **強制 90 度轉角**：所有線段必須為純水平或純垂直，即時偵測並標示違規線段；存在斜線時封鎖確認 / 退出
- **物流橋強制插入**：路徑跨越現有管線時自動插入，無法關閉
- **分流器 / 匯流器預設插入（Phase 2）**：可由使用者切換為截斷模式（見 2.6）

---

## 4. UI 規格

### 4.1 管線視覺樣式

| 狀態 | 傳送帶 | 水管 |
|------|--------|------|
| 一般 | 橘色實線＋方向箭頭 | 藍色實線＋方向箭頭 |
| 高亮（管線模式） | 亮橘色加粗 | 亮藍色加粗 |
| 有 Error | 紅色閃爍邊框 | 紅色閃爍邊框 |
| 有 Warning | 黃色邊框 | 黃色邊框 |
| 懸停 | 顯示流量 tooltip | 顯示流量 tooltip |

### 4.2 接口視覺樣式

| 狀態 | 樣式 |
|------|------|
| 一般 | 灰色小圓點 |
| 管線模式高亮 | 白色圓點＋脈衝動畫 |
| 可吸附（接近中） | 綠色放大圓點 |
| 已連接 | 對應 type 顏色實心點 |

### 4.3 快捷鍵彙整

| 快捷鍵 | 功能 |
|--------|------|
| P | 切換管線模式 |
| Escape | 取消當前管線繪製 |
| Delete | 刪除選取管線 |
| 點選空格（繪製中） | 新增彎折點 |

---

## 5. 驗證方式

| 驗證項目 | 方法 |
|----------|------|
| 管線模式切換 | 按 P 鍵確認接口與管線高亮出現 / 消失 |
| Type 自動判斷 | 從 conveyor 接口拉線，確認為傳送帶樣式；從 pipe 接口拉線，確認為水管樣式 |
| 不同 type 無法互連 | 嘗試將傳送帶接至水管接口，確認出現 Error |
| 吸附至接口 | 拖曳管線終點接近接口，確認自動吸附 |
| 移動設備自動連接 | 移動設備使接口與既有管線端點對齊，確認自動建立連接；type 不符時確認不連接 |
| 手動彎折點 | 繪製時點選中途空格，確認彎折點產生，路徑正確轉折 |
| 斜線鎖定確認 | 放置彎折點使線段出現斜線，確認彎折點變紅且無法確認放置 |
| 斜線修正後解鎖 | 修正彎折點至合法位置，確認可正常確認放置 |
| 已確定路徑與游標線段區分 | 繪製中點選一個彎折點後移動滑鼠，確認起點到該彎折點的路徑樣式不再變動，僅游標到該點之間的路徑持續即時更新且樣式可明顯區分 |
| 已放置管線不可編輯 | 點選已放置管線，確認彎折點無法拖動，僅可刪除 |
| 分流器預設自動生成（Phase 2） | 從管線中途拉出新管線，確認預設插入分流器，原管線一分為二 |
| 分流器切換截斷模式（Phase 2） | 點選預示分流器圖示切換，確認改為截斷，原管線起源段保留，新管線接出 |
| 匯流器預設自動生成（Phase 2） | 將管線終點拉至現有管線中途，確認預設插入匯流器 |
| 匯流器切換截斷模式（Phase 2） | 點選預示匯流器圖示切換，確認改為截斷，原管線起源被新管線取代 |
| 物流橋強制插入 | 規劃跨越現有管線的路徑，確認自動插入物流橋且無切換選項 |
| 管線刪除觸發流量重算 | 刪除管線後確認流量估算即時更新 |
| 管線刪除警示以設備為主 | 刪除原本兩端均未接有效設備的管線，確認不新增警示 |
| 管線刪除產生設備警示 | 刪除後某設備失去輸入來源，確認對應設備出現 E004 |
| 點選管線顯示資訊面板 | 點選單一管線，確認物件資訊面板顯示 type、兩端設備接口、流量與警示狀態 |
| 管線隨設備框選 | 框選同時涵蓋設備與管線，確認框選範圍內管線與設備一併進行後續編輯 |
| 管線移動 | 移動已放置管線，確認彎折點隨之平移且仍維持 90 度轉角限制 |
| 管線移動脫離接口 | 移動管線使端點偏離原接口，確認連接解除、對應設備重新評估並於失去輸入來源時出現 E004 |
| 管線複製 | 複製已放置管線，確認彎折點與自動生成節點一併複製，且複製後不連接至任何接口 |

---

*本文件為 CR-02 Feature Spec，系統整體架構見 `00_top_spec.md`。*
