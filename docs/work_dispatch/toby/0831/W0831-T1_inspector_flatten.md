# W0831-T1｜toby｜Inspector 攤平（R-B4 續作）

| meta | value |
|------|-------|
| 週次 | 2026-08-31 → 2026-09-06 |
| 對應 roadmap | [R-B4](../../../roadmap/detail/B4_selection_inspector.md)（細項給決策用；**做法以本檔＋GUIDE 為準**） |
| 等級 | **加分**（不列 9/27 必要） |
| 擋門檻 | **否** |
| 性質 | 接線（**本週只做這一種**） |
| 預估時數 | **3–5h**；一塊吃完即停 |
| review_gate | dernoson |
| mentor | dernoson（分層／Vue）；機器／配方資料問 aaaaa |
| **先讀** | [GUIDE_inspector_flatten](./GUIDE_inspector_flatten.md)（白話＋可貼片段＋四條指令） |
| 工單風格 | 白話目標在前；照檔改；環境指令寫死 |
| 狀態 | `[ ]` 未開始 |

---

## 0. 白話目標（先看這三句）

上週點設備，左邊已經看得到名稱、佔格、耗電——那塊**不要弄丟**。

這週只改一件事：畫面不要再直接抱著整包機器資料。先算出「名稱、佔格文字、耗電文字」這幾個字，再拿去顯示。

做完看起來跟現在幾乎一樣。不一樣的是：以後別人要做純顯示元件時，只要接這幾個字，不必再去翻 store。

---

## 1. 四欄

| 欄 | 內容 |
|----|------|
| **畫面** | 點一台設備：仍見名稱、佔格、耗電。點空白：「未選取設備」。能加一行配方名稱更好，加不到也算完成 |
| **交哪個檔** | **主要這一支：** `src/editor/inspector/InspectorPanel.vue`。若要拆顯示元件，新檔只能放 `src/editor/inspector/` |
| **不要碰** | `ToolbarPanel.vue`（aaaaa 在改）、畫布／overlay、store **函式簽章**、巨型說明檔進 repo |
| **卡住找誰** | 分層／紅字：dernoson。佔格或配方數字怪：aaaaa。**卡超過一次就問，不要硬撐** |
| **對照** | 你上週自己的 PR #33：同一個檔、單一主題、可截圖——節奏照抄 |

---

## 2. 名詞（只會碰到這些）

| 詞 | 白話 | 你要做的 |
|----|------|----------|
| **攤平** | 把整包物件收成畫面要的幾個字 | 見 GUIDE §1–§3 |
| **容器（這個檔）** | 可以讀 store、可以 `getMachine` | 查資料留在這裡沒問題 |
| **顯示** | 只吃字串／數字 | template 改綁攤平後的物件，不要再寫 `selectedMachine.width` |
| **`getRecipesForMachine`** | 用機器**中文名**查配方 | 加分才用；從 `@/data/products` import |

本週**不要**做：改配方、改名稱、刪除按鈕、多選彙總、改工廠寬高那一段。

---

## 3. 開工（約 10 分鐘）

- [ ] 本機 `pnpm dev` 能開
- [ ] 打開並**只讀**：`InspectorPanel.vue`（你上週加的「設備資訊」區塊）、GUIDE §2 的型別
- [ ] 確認不開第二個功能

（先報檔名改為建議，不是必做。）

---

## 4. 步驟

1. 照 [GUIDE §3](./GUIDE_inspector_flatten.md) 加上 `deviceInfo` computed  
2. template 三個欄位改綁 `deviceInfo`  
3. （有剩再做）GUIDE §4 配方一行  
4. 跑 GUIDE §5 四條指令  
5. 截圖或短錄影：選一台／點空白各一次  

若你認為「不該拆物件、該整檔重寫」——**先回報**，不要自己改 store。

---

## 5. DoD

- [ ] 名稱、佔格、耗電行為不回退
- [ ] 空選仍正確，不報錯、不留上一台的字
- [ ] 顯示不再直接綁 `selectedMachine.xxx`（工廠寬高／snap 除外）
- [ ] 未改 `ToolbarPanel`、未改 store 簽章
- [ ] 四條 `pnpm` 指令通過
- [ ] （加分）配方名稱至少一行字

---

## 6. 未交頂替

維持上週已合入行為；攤平延 9/13。不計失敗。

---

## 7. 回報

| 時機 | 動作 |
|------|------|
| 卡住 >1 次 | 立刻問 dernoson／aaaaa |
| 週中 | 可能 ping 一次（預期內） |
| 完成 | PR ＋截圖 |
