# W0907-P1｜paper｜frame 標號＋本週可動用的 icon 清單

| meta | value |
|------|-------|
| 週次 | 2026-09-07 → 2026-09-13 |
| 等級 | 加分（**但本週有下游在等**） |
| **deadline** | **2026-09-09（週三）23:59**——你週四～六出門，所以壓在出門前 |
| 份量 | **1 小時以內** |
| 交付方式 | Discord 貼給主編即可，**不用碰 git** |

---

## 1. 要什麼

兩件事，都是清單，不用畫新東西。

### (1) frame 標號

上週這項你回報完成，但 repo 裡沒有東西可以對照。這次**不需要你進 repo**——把 Figma 裡目前的 frame 逐一給一個編號與名字，貼成一則訊息就好，像這樣：

```text
F-01 主畫面（工廠視角）
F-02 下方工具列
F-03 右側資訊面板
F-04 警告／錯誤欄
...
```

實作端要引用時就講「F-02 那張」，不會再出現「工具列那張是哪張」的來回。

### (2) 本週可動用的 icon（**新增，有人在等**）

avery 這週要刻 2–3 個小按鈕。請從你現有的稿裡**挑三個 icon**，給我：

- icon 名稱（例如「刪除」「旋轉」「吸附」）
- 它在哪個 frame 裡
- 該 icon 的 SVG 或 copy-as-CSS

**挑最不重要的三個就好**——這是練手用的，做壞了不影響任何東西。

---

## 2. 凍結

你挑出來的那三個 icon，以及 **F-02 工具列那張**，本週進入**凍結**：

> 有人開始實作的 frame，設計要改須在週會前公告，並明列會作廢哪些工項。

上週 goodmorning 的 `MachineCard` 就是被視覺迭代作廢的——**那不是你的錯**（迭代三塊視覺正是你的工單），是流程缺凍結點。所以現在有了這條。**要改就改，只要先說一聲。**

其他 frame 不凍結，照常迭代。

---

## 3. 不做

- 任何 `.vue`／`.ts`／JSON。
- 不用整理檔名、不用 clone repo、不用開 PR。
- 不用畫新的稿。

---

## 4. DoD

- [ ] 9/09 前 Discord 貼出 frame 編號清單
- [ ] 同時貼出三個可動用 icon（名稱＋所在 frame＋SVG）
- [ ] 主編代為存進 `docs/paper/`

---

## 5. 未交頂替

不計失敗。avery 的工單會改成「從既有 `src/components` 挑一個既有樣式仿作」，不影響其他人。
