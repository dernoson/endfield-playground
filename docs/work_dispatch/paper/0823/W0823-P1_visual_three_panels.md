# W0823-P1｜paper｜三塊畫面視覺基準（對齊月）

| meta | value |
|------|-------|
| 週次 | 2026-08-23 → 2026-08-30 |
| 對應 roadmap | 上游三塊畫面基準；為 [R-B1](../../../roadmap/detail/B1_toolbar_real_machines.md)／[R-B4](../../../roadmap/detail/B4_selection_inspector.md)／右側警訊（[R-D1](../../../roadmap/detail/D1_stats_item_summary.md)、[R-D2](../../../roadmap/detail/D2_e001_overlap_alert.md)）預留視覺對齊 |
| 等級 | **確定**（**不**列 8/30 技術門檻） |
| 擋 8/30 門檻 | **否**（未交則工程沿用現有 UI；9/6–9/20 再套稿） |
| 性質 | 視覺／Figma（**本週只做這一種；零程式**） |
| 預估時數 | **3–5h**（晚間窗口即可） |
| review_gate | dernoson（稿是否可合入 `docs/paper/`）；工程落地另找對接人（見 §7） |
| 狀態 | `[ ]` 未開始 |

---

## 0. 一句話

把主視覺稿對準「模擬器打開後使用者看到的三塊區域」，標出 **2–3 個可截圖的關鍵 frame**，讓下週起做選單／資訊欄的人知道要長什麼樣——**你不必寫任何程式、也不必跑 `pnpm`**。

---

## 1. 四欄工單

| 欄 | 內容 |
|----|------|
| **畫面** | Figma 裡能清楚看出：下方設備選單、中央畫布（可簡化）、右側數字／警訊區的版面與層級；至少 2 個 frame 可單獨截圖當「完成長相」 |
| **交哪個檔** | 更新 `docs/paper/` 既有 `.fig`（目前主檔名見該資料夾）；Discord 貼 Figma 連結或截圖；可選在 `docs/paper/README.md` 用兩行列出「本週關鍵 frame 名稱」 |
| **不要碰** | 任何 `.vue`／`.ts`、JSON、圖檔命名 bulk、要求自己 clone 跑專案、替工程師寫元件 |
| **卡住找誰** | dernoson（稿範圍／合入）；產品「這週要對哪三塊」問 aaaaa；**落地成程式不是你的事**（見 §7） |

---

## 2. 為何是你、範圍怎麼切

| 依據 | 結論 |
|------|------|
| 本週可做 | UI 視覺稿（Figma） |
| 本週不做 | **任何程式**、資料整理、數學 → 硬禁 |
| 時數規劃 3–5h | 只做「對齊三塊＋標關鍵 frame」，不做整本 UI kit／全元件庫 |
| 要避開的失敗模式 | Figma 權限／匯出摩擦 → 允許用既有 workaround；以能進 repo 的 `.fig` 為準 |
| 專案風險 | 稿若無人翻成元件會停在 `docs/paper/` → 本週工單**標註對接對象**，但不要求你教他們寫 code |
| 沿用現況 | 你既有的「只推 `.fig`、不碰程式」節奏維持不變 |

**本週不做：** 正式機器立繪、管線精緻風格（10 月 C3）、完整 design token 進 `src/`、Vue 教學。

---

## 3. 產品詞對照（給設計用，不是程式課）

模擬器介面在 roadmap 裡固定講「三塊」——請以這三塊為本週對齊範圍：

| 你畫的區域 | 使用者在幹嘛 | 之後誰可能照著做（你不用管實作） |
|------------|--------------|----------------------------------|
| **下方選單** | 挑要放哪一台機器 | 約 9/6 起：工具列真機器（B1）；L3 可能是 goodmorning／MBD |
| **右側資訊／警訊** | 看產耗數字、重疊等警告文字 | 資訊欄（B4）、產耗（D1）、重疊警訊（D2） |
| **頂欄（含 Save 區）** | 切工具、看檔名、之後存取藍圖 | Navbar 已有選取／移動鈕，本週 harry 會再加「管線」；藍圖存檔 UI 排 11 月（D4） |

中央畫布維持示意即可（互動是 L2 的事）；本週你的守備範圍就是上表這三塊。

外觀聽你；**功能做多少、哪週上線**聽 roadmap（主編已定）。若稿上的功能超前（例如完整藍圖存檔 UI），標成「以後」即可，**不要**要求工程本週做完。

---

## 4. 開工前檢查

- [ ] 打開既有主稿：`docs/paper/` 內 `.fig`（檔名可能含中文；以資料夾現況為準）
- [ ] 有 Figma 可編輯權限；若匯出卡住，用你已知 workaround，目標仍是**更新進 repo 的那份 `.fig`**
- [ ] **不需要**安裝 Node／跑 `pnpm dev`（本單不要求）
- [ ] 確認本週只畫／改「對齊用」畫面，不開新的程式相關任務給自己

---

## 5. 步驟（對齊 3–5h）

### 5.1 對齊三塊（建議 ≤3h）

1. 在主稿中標出或整理：**下方選單**、**右側資訊／警訊**（中央畫布可維持示意）
2. 選單側請至少表達：分類感（Tab 或分區）、卡片上有**名稱**與**佔格示意**（例如「3×2」文字即可；**正式圖資源本週不做**）
3. 右側請至少表達：標題區＋一條「警訊／提示」列的層級（文案可用假字；重點是層級與位置）

### 5.2 標出可驗收 frame（≤1h）

依 A4「設計稿 30 秒驗收」：別人不必開 IDE，只看一張圖就知道你做完了。

請在稿內（或 Discord）點名 **2–3 個 frame**，例如：

| # | 建議 frame | 用途 |
|---|------------|------|
| 1 | 下方選單（含 1–2 張假卡片） | 給 B1／L3 對齊；本週 goodmorning 正在做卡片 mock |
| 2 | 右側資訊或警訊列表 | 給 B4／D1／D2 對齊；本週 MBD 要做右側空狀態，有 frame 他就照著做 |
| 3 | （可選）頂欄或整頁三塊總覽 | 給週會演示 |

### 5.3 交付進 repo

1. 更新 `docs/paper/*.fig` 後交 PR 或依你慣例推分支請 dernoson 合入  
2. Discord 貼：Figma 連結 **或** 上述 frame 截圖  
3. （可選）`docs/paper/README.md` 兩行寫：「本週關鍵 frame：…」——兩行就夠，**不要**寫成長文

Commit 訊息可用中文描述設計變更；若仍不熟 git，把檔給 dernoson 代推也可，但**內容必須是你的稿**。

---

## 6. DoD

- [ ] `docs/paper/` 主 `.fig` 有本週更新（或同等可開的 Figma 連結，且團隊找得到）
- [ ] 三塊區域在稿上可辨識（下方／中央示意／右側）
- [ ] 至少 **2** 個關鍵 frame 被點名（Discord 或 README）
- [ ] **沒有**任何你提交的 `.vue`／`.ts` 變更
- [ ] 30 秒驗收：外人看截圖能指出「選單」與「右側」兩區

---

## 7. 未交頂替與落地對接

| 情況 | 處理 |
|------|------|
| 本週稿未交 | **不擋 8/30**；工程沿用現有 UI；B1 明寫稿未到就用現樣式（見 B1 §8） |
| 稿有了但沒人做成元件 | 專案級風險；對接候選：toby（接過 Navbar）、harry（有意接 UI，不含美術）、goodmorning／MBD（單檔 mock）——**由 dernoson／aaaaa 指定，不是你本週任務** |

---

## 8. 回報

| 時機 | 動作 |
|------|------|
| 關鍵 frame 定好 | Discord 丟截圖／連結即可（當天回訊即可）；**短語音也行** |
| 合入 | @ dernoson |
| 工程問「這個按鈕要接什麼資料」 | 轉 aaaaa／dernoson；你只定外觀與層級 |

---

## 9. 開發日誌（派工側）

### 2026-08-23

- 依 roadmap 三塊畫面基準正式派工；設計域、零程式、3–5h
- 對齊 B1／B4／D1／D2 的「稿不擋門檻、但要標 frame」節奏
- 附產品三塊對照；不教程式
