# 本週派工大綱｜2026-09-21 → 2026-09-27（**M2 門檻週**）

| meta | value |
|------|-------|
| version | **v1.2（2026-09-24；G1 恢復交付＋MBD 產能裁示）** |
| 門檻日 | **2026-09-27（日）＝ M2 擺放月門檻，硬綁 B1** |
| 正式依據 | 本檔＋`work_dispatch/<code>/0921/` |
| 上游 | [ROADMAP_OUTLINE](../roadmap/ROADMAP_OUTLINE.md) **v1.13**、[WEEK_20260914](./WEEK_20260914.md) v1.1 |
| 前置狀態 | **技術前置全數在 master：** `layoutStore` #45、viewport #47、只讀殼 #46、主畫面接入 #50、C2／D4 契約 #51。**待審實質 0**（#48 已裁定維持現狀，不列合入序、不計待審） |
| 本週人力 | 可驗證約 **14–22h／7 人**；扣掉守門後功能約 **12–19h／6 人**。**v1.2 起有交付要求者 6 人**（G1 恢復） |
| 不派 | avery（亞運至約 10 月初）、azure9572（備賽至 11 月）、**MBD（有 3–5h 產能，本週仍不派新功能碼——見 §2.0.1）** |
| **本版狀態** | 9/23 七項裁決已回寫；**9/24 追加兩項追裁**（§2.0.1） |

---

## 0. 本週目標（30 秒）

**只有一條線要成立：從下方選單點一台真機器，放到畫布上，那台機器出現。** 這就是 9/27 的 M2 門檻演示。

1. **最優：** aaaaa 把 `layoutStore` 的私有檢查提成純函式 `canPlaceDevice`（[A0](./aaaaa/0921/W0921-A0_placement_precheck.md)），**9/24 交**。
2. **主戲：** toby 接落子鏈——工具列意圖 → 點畫布 → `addDevice`（[T1](./toby/0921/W0921-T1_placement_chain.md)）。**只做點擊落子；拖曳與綠紅框是加分。**
3. **清路：** shirone 把「純搬家」那一筆單獨拆一支 PR 合進去（[S1](./shirone/0921/W0921-S1_stats_panel_split_pr.md)），**另接手 MBD 原本的面板樣式**（無死線）。
4. **清積壓：** goodmorning **本週必須**把 #48 修完合入（[G1](./goodmorning/0921/W0921-G1_toolbar_pr48_land.md)，**9/26 前可審**）——換回真實機器資料、保留視覺；不擋 T1，但不得再無限期掛著。
5. **不在門檻上：** harry 做 C3 折線幾何提前量（[H1](./harry/0921/W0921-H1_pipeline_polyline.md)）；paper 出落差清單（[P1](./paper/0921/W0921-P1_figma_comment_gap.md)）。
6. **有產能、不派新功能：** MBD（[M0](./MBD/0921/W0921-M0_pause.md)）——週報自報約 3–5h，但本週不給新產品碼；可選 Storybook 上手。

**上週五項確定工項全數合入，是 roadmap 開跑以來第一次。** 換殼完成；9/23 裁決後門檻鏈只剩 A0＋T1。**9/24 起多一條清積壓線（G1），與 T1 同改 `ToolbarPanel.vue`——分區鎖要守住。**

---

## 0.1 本週驗收（9/27）

| # | 驗什麼 | 過關長相 | 誰 | 擋門檻 | 狀態 |
|---|--------|----------|-----|--------|------|
| **V1** | **落子鏈打通** | `pnpm dev` → 點工具列真機器 → 點畫布空格 → 那台機器以正確佔格與**中文名**出現 | toby T1 | **是（M2）** | [ ] |
| V2 | 落子前預檢 | `canPlaceDevice` 在 master；`layoutStore.test.ts` **未被修改**且全綠 | aaaaa A0 | **是**（T1 前置） | [ ] |
| V3 | 工具列 #48 合入 | #48 進 master；真實機器資料來源未退回；視覺／stories 可用 | goodmorning G1 | 否 | [ ] **（v1.2 恢復）** |
| V4 | 產線總覽路徑 | `src/app/StatsPanel/` 進 master；diff 只有 10 個 rename ＋ 一行 import | shirone S1 | 否 | [ ] |
| V5 | 閘門 | 待審 ≤3；無選取／旋轉／刪除被放行 | dernoson D0 | 否 | [ ] |
| V6 | C3 折線幾何 | `/dev/pipeline-polyline` 可開；`valid` 與 `isAxisAlignedPath` 一致 | harry H1 | 否（提前量） | [ ] |
| ~~V7~~ | ~~MBD 乾淨分支~~ | — | — | — | **取消（R-2）：** M1 作廢，`dev/MBD` 不合入 |
| V8 | 設計落差清單 | 每條有「稿上 / 現在 / 差在哪」 | paper P1 | 否 | [ ] |

> **V7 仍取消**（M1 作廢）。**V3 於 v1.2 恢復。** 0928 結算分母＝V1／V2／V3／V4／V5／V6／V8。S1 樣式那半不下死線、不列驗收。

**V2 未達成：** T1 改用「放下去才知道」（直接 `addDevice`，失敗不落子），**V1 仍可成立**，但拖曳體驗降級且有兩套判定的風險。
**V1 未達成：** 9/27 當日記錄「工具列真機器列表已在 master（#43），落子鏈順延 10 月」，B1 降級並在 [ROADMAP §9](../roadmap/ROADMAP_OUTLINE.md) 回寫延因。**不做另外的降級儀式。**

### 0.2 驗收速查

#### A0 — 落子前預檢

| 步驟 | 預期 |
|------|------|
| `pnpm type-check`＋`pnpm test` | 全綠 |
| `git diff src/__tests__/store/layoutStore.test.ts` | **空**（store 行為不變的證據） |
| `grep -n "editorStore" src/utils/layout/placementCheck.ts` | 零命中 |
| diff | 不含 `src/editor/*` |

#### T1 — 落子鏈

| 步驟 | 預期 |
|------|------|
| `pnpm dev` → 點真機器 → 點空格 | 出現設備，標籤為中文名，佔格正確 |
| 點已佔用的格 | 什麼都沒發生（A0 合入後） |
| `grep -n "store\|vue-flow" src/editor/layout/GridCanvas.vue` | 零命中 |
| `grep -n "detectOverlaps" src/editor/` | **零命中**（不得自算重疊） |
| `ToolbarPanel.vue` 的 diff | **只在 `<script>` 區** |

#### G1 — 工具列 #48

| 步驟 | 預期 |
|------|------|
| 資料來源 | 仍為 `listToolbarMachines`／既有 tags，**不是** hardcode 陣列 |
| 視覺／stories | 可用；`pnpm storybook` 開得起 ToolbarPanel |
| 與 T1 | 若 master 已有 `arm`／落子意圖，**不得刪掉** |
| 交期 | 9/26 前可審；9/27 前合入或明寫卡點 |

#### S1 — 產線總覽搬家（樣式那支不在此表）

| 步驟 | 預期 |
|------|------|
| `git diff --stat origin/master...` | 10 個 `R100` rename ＋ `MainLayout.vue` 一行 |
| 夾帶新元件／新字型／新 story／樣式改動 | **退回重拆** |
| `pnpm dev` 右側 | 總覽區塊仍在 |

---

## 1. 派工總表

| ID | 人 | 狀態 | 性質 | 擋門檻 | 細項 | 未交的話 |
|----|----|------|------|--------|------|----------|
| **W0921-A0** | aaaaa | **確定（最優）** | 純函式（L1） | **是** | [A0](./aaaaa/0921/W0921-A0_placement_precheck.md) | T1 退為「放下去才知道」 |
| W0921-A1 | aaaaa | 次優（可超前） | 純函式（L1） | 否 | [A1](./aaaaa/0921/W0921-A1_connect_rules.md) | 回到原排程 10/04，零影響 |
| **W0921-T1** | toby | **確定（主戲）** | 接線（L2） | **是** | [T1](./toby/0921/W0921-T1_placement_chain.md) | B1 於 9/27 降級 |
| W0921-D0 | dernoson | **確定** | 守閘／合入 | 否 | [D0](./dernoson/0921/W0921-D0_gate_and_three_rulings.md) | 合入序停擺 |
| W0921-G1 | goodmorning | **確定（清積壓）** | 畫面（L3）＋資料來源復原 | 否 | [G1](./goodmorning/0921/W0921-G1_toolbar_pr48_land.md) | 9/26 無動靜則代修後合入或關閉 |
| W0921-S1 | shirone | **確定（搬家）＋無死線（樣式）** | 路徑整理＋畫面（L3） | 否 | [S1](./shirone/0921/W0921-S1_stats_panel_split_pr.md) | 右側維持舊路徑，D1 十一月切片無落腳點 |
| W0921-H1 | harry | 確定（**不綁時間點**） | 純函式＋dev 頁 | 否 | [H1](./harry/0921/W0921-H1_pipeline_polyline.md) | C3 回原排程 10/11 |
| W0921-M0 | MBD | **不派新功能**（有 3–5h） | 可選學習 | 否 | [M0](./MBD/0921/W0921-M0_pause.md) | — |
| W0921-P1 | paper | 加分 | 設計文件 | 否 | [P1](./paper/0921/W0921-P1_figma_comment_gap.md) | 工程師照舊查 Figma comment |
| W0921-V0 | avery | **暫停** | — | 否 | [V0](./avery/0921/W0921-V0_pause.md) | — |
| W0921-Z0 | azure9572 | **暫停** | — | 否 | [Z0](./azure9572/0921/W0921-Z0_pause.md) | — |

**配對名額：** **toby**（T1）。第二個名額本週空著。

---

## 2. 定案

### 2.0 裁決結果（2026-09-23，主編）

v1.0 在裁決未回傳的情況下依建議選項先發。**裁示已於 9/23 給出，七項中有四項與預設不同**，以下為實際結論，本檔與所有工單、roadmap 皆已據此改寫。

| # | 裁示 | v1.0 預設 | 影響 |
|---|------|-----------|------|
| **R-1** | **駁回。** 兩套新面板只在 `dev/shirone0918`、沒開 PR、不在 master；master 只有一套，**不存在「三套要選」** | 「本週先不裁」 | S1 §4 改寫；那兩個目錄改定位為**參考素材** |
| **R-D1 封鎖** | **撤銷。** 理由同 R-1 | （v1.0 寫進 roadmap §9） | 大綱 §9 該列已刪，D1 細項回寫 |
| **R-2** | **不合，M1 作廢。** MBD 的樣式工作已由 shirone 接手 | 「樣式收、由他重推乾淨分支」 | M1 → [M0 暫停單](./MBD/0921/W0921-M0_pause.md)；V7 取消 |
| **S1 範圍** | **擴大：** 搬家 ＋ MBD 原本的樣式設計，兩件都要做完 | 只有搬家 | S1 升 v1.1，切成 A／B 兩支 PR（見 §2.1） |
| **R-3** | **#48 維持現狀，不合也不關。** G1 不再擋 T1 | 「裁掉非視覺 diff 後 9/24 前合入」 | G1 全單改寫；T1 第 2 刀解除依賴；V3 取消 |
| **R-4** | **同意開放點擊落子。** 選取／旋轉／刪除不開 | 同 | 無變動 |
| **§4 第 3 條** | **不採納**（待審停滯 3 天點名） | 列為新規則 | 已從 §2.1／§3／§4 移除 |

> **R-4 的理由（維持 v1.0 的記載）：** [ROADMAP §11](../roadmap/ROADMAP_OUTLINE.md) 寫明「底層就緒不等於自動解鎖」，本檔沒有由「依賴已清」推論。放行是因為 9/27 硬綁 B1，而 B1 的門檻句就是「拉真機器放到畫布」——不放行等於門檻當日直接降級。**A0 不需要這道放行**（純函式重構，即使否決 L2 落子也該做）。

**R-3 的合入條件（寫進 [B1 細項](../roadmap/detail/B1_toolbar_real_machines.md)）：** #48 日後要合時由 goodmorning 自行 rebase，且須把寫死的機器清單換回真實機器資料——目前該分支把 `listToolbarMachines` 換成了 hardcode 陣列，直接合入等於把 B1 的 9/6 切片退回去。

#### 後記（v1.1）：兩個人本週曾沒有交付項

M1 作廢、#48 曾裁定擱置之後，MBD 與 goodmorning 一度都沒有要交的東西。**v1.2 已改：G1 恢復交付；MBD 維持不派功能碼（見 §2.0.1）。**

### 2.0.1 追加裁示（2026-09-24）

| # | 裁示 | 依據 | 影響 |
|---|------|------|------|
| **R-3′** | **#48 本週必須修完合入**（不再「維持現狀無限期擱置」）。T1 仍不依賴它；G1 與 T1 同檔按區塊分鎖 | 主編：拖太久了 | [G1](./goodmorning/0921/W0921-G1_toolbar_pr48_land.md) v1.2；V3 恢復；合入序補回 G1 |
| **MBD 產能** | 週報自報下週「與本週差不多」＝約 **3–5h，有時間**。**本週仍不派新產品功能**——StatsPanel 已轉出，且 Storybook／工作流尚未確認前不宜再給共檔 L3 | 週報 0914 ＋主編保守態度 | [M0](./MBD/0921/W0921-M0_pause.md) v1.2；可選 [Storybook GUIDE](./MBD/0921/GUIDE_storybook_first_look.md) |

### 2.1 本週生效的定案

| 項 | 結論 |
|----|------|
| **落子範圍** | **點擊落子**過關即可。拖曳（HTML5 DnD）與綠框／紅框即時預覽列為加分，不進驗收 |
| **落子意圖住哪** | 新開 `src/editor/toolbar/usePlacementIntent.ts`（module-scope ref）。**不擴充 `EquipmentType`、不改 `editorStore`**——那會把新佈局模型綁回舊藍圖世界，而那條邊界待決（B-1） |
| **`label` 填什麼** | **`machine.name`（中文名）**。`GridCanvas` 畫的是 `label ?? machineType`，不填的話門檻演示會看到一排英數 id |
| **`createPlacedDevice` 工廠** | **本週不做**，由落子端自己組欄位。工廠的預設值屬呈現決策（待決 B-2），排 10 月 |
| **預檢不得重算** | L2 不得自行 import `detectOverlaps`／`toDeviceFootprint`／`deviceSizeFromMachine`。三支都是 public export，做得到但**退回** |
| **`ToolbarPanel.vue` 分區** | **`<script>`＝toby（落子意圖）／`<template>`＋`<style>`＋stories＝goodmorning。** G1 為換回真實資料可動 script 的**資料來源那幾行**，但**不得刪 `arm`／意圖層**。兩邊同週改此檔，衝突找 dernoson |
| **`MainLayout.vue`** | owner **toby（全檔）**。唯一例外：shirone 可改 StatsPanel 的 import 那一行，其餘一字不動 |
| **「paper 過」** | 不再是任何 PR 的**合入前提**，一律改事後補審。低時數成員的簽核比照 |
| **S1 拆兩支 PR** | **A ＝搬家**（本週必交，≤2h）、**B ＝樣式**（無死線）。同一支夾帶兩者＝退回重拆 |
| **StatsPanel 單一 owner** | **shirone**（元件、路徑、樣式全包）。不得新開第三個目錄；`ItemSummaryTable.vue` 的 props 與 PR #37 的空狀態必須保留 |
| **`InspectorSidebar`** | **凍結：誰都不能刪。** 它是 R-B4 的呈現端入口，10 月要用 |
| **B3／B4／B5** | 本週**全部不開**。旋轉、選取、刪除任一被送審即退回 |
| **#48** | **本週收尾合入（R-3′）。** 硬條件：rebase ＋ 資料來源換回真實機器；視覺／stories 保留。**不擋 T1**，但進合入序與 V3 |
| **鎖表公告** | WEEK §3 整段貼 Discord；**暫停者的工單也附鎖清單**（規則 23） |

### 2.2 9/27 必要項

| 必要項 | 狀態 | 說明 |
|--------|------|------|
| **B1** | **硬綁** | 9/6 切片已合（#43：分類 Tab＋真名＋佔格）。整包缺的就是落子鏈＝本週 T1 |
| B2 | 不列必要 | 本週放行開工，但**技術前置 9/20 才全數到齊、只剩一週**，維持不硬綁（9/15 撤回擴大後未再變更） |
| B4 | 不列必要 | 選取本週不開；`selectionStore` 歸屬待決（B-1） |

**本週擋門檻的唯一一條鏈：`T1 落子`（工具列 script → 畫布 → `addDevice`）＋ `A0 預檢`。** R-3 之後 #48 已不在鏈上，這條鏈只剩 toby 與 aaaaa 兩個人。其餘全部不在門檻上。

---

## 3. 合入優先

**S1 搬家 → A0 → T1 → G1（#48）→ H1 → A1 →（S1 樣式，無死線）。** 待審 ≤3。

**G1 與 T1 同檔：** T1 優先合 script（落子）；G1 rebase 其上再合視覺＋資料來源復原。若順序反了，G1 不得覆蓋掉已合入的 `arm`。

**退回：** 選取／旋轉／刪除接線、擴充 `EquipmentType`、改 `editorStore` 簽名、L2 自算佔格重疊、`GridCanvas` 內 store import、刪 `InspectorSidebar` 或 `FactoryCanvas`、S1 的搬家 PR 夾帶樣式、新開第三個 StatsPanel 目錄、#48 仍帶 hardcode 機器清單當正式資料源、非 owner 改 `MainLayout.vue`、`docs/paper/` gitlink、網頁 Upload、檔名當版本、不可見字元。

### 檔案鎖

**本表同步貼 Discord；暫停者亦適用。**

| 區 | owner | 別人不要碰 |
|----|-------|-----------|
| `src/utils/layout/placementCheck.ts`、`src/store/layoutStore.ts` | **aaaaa** | 全員 |
| `src/utils/layout/connectRules.ts`、`resolveConnections.ts` | aaaaa（次優） | 全員 |
| `src/editor/layout/LayoutView.vue`、`GridCanvas.vue` | **toby** | 全員（含 harry） |
| `src/editor/toolbar/usePlacementIntent.ts` | **toby** | 全員 |
| `src/editor/toolbar/ToolbarPanel.vue` | **`<script>` 意圖層＝toby；資料來源復原＋`<template>`／`<style>`／stories＝goodmorning** | 其餘全員。兩邊同週改：衝突找 dernoson，**禁止互蓋** |
| `src/app/layouts/MainLayout.vue` | **toby（全檔）** | 全員。**例外：** shirone 限改 StatsPanel import 一行 |
| `src/app/StatsPanel/*`、`src/components/StatsPanel/*` | **shirone（元件＋路徑＋樣式，單一 owner）** | 全員，**含 MBD** |
| `src/app/shirones_StatsPanel/*`、`src/app/test_StatsPanel/*` | shirone（分支上的參考素材，**不進 master**） | 全員 |
| `src/utils/layout/pipelinePolyline.ts`、`src/app/dev/PipelinePolylineDemo.vue` | **harry** | 全員 |
| `src/utils/layout/pipelineGeometry.ts` | 唯讀參考 | **不得改既有簽章** |
| `src/editor/inspector/*`、`InspectorSidebar` | **凍結** | **誰都不能刪**（R-B4 入口） |
| `FactoryCanvas.vue`、`FlowNodeOverlay.vue` | 凍結 | 保留、不加深、不刪 |
| `editorStore` 簽名 | 凍結 | 全員 |

---

## 4. 上週留下的三個教訓（本週已對應處置）

| # | 上週發生什麼 | 本週怎麼改 |
|---|-------------|-----------|
| 1 | `MainLayout.vue` 被三個人各改一份，其中兩人是暫停中、沒收到鎖表 | **暫停單也附鎖清單**；鎖表貼 Discord；`MainLayout` 改為 toby 全檔鎖，例外逐條寫明 |
| 2 | #48 的合入前提是「paper 過」，而 #48 七天沒動 → 兩邊互等 | **簽核一律改事後補審**，不再列為任何 PR 的合入前提 |
| 3 | S1 工單只要搬家，回來的是搬家＋兩套工單沒要的面板，且整支沒開 PR | **「不要做的事」寫成清單而非一句話**，並在工單明文寫「若你認為還該做 X，先講再做」 |

> v1.0 曾在第 2 條後面附加「待審停滯 3 天即 Discord 點名」，**主編 9/23 不採納**，已從本檔與 [R-E2](../roadmap/detail/E2_layer_guard_pr_rules.md) 移除。
>
> 另補一條結算口徑（同日裁示）：**沒開 PR 的分支內容不算交付物。** v1.0 把 shirone 分支上那兩個目錄記成「交了三套」並據此開封鎖，是判讀錯誤——「沒交」與「交了不能合」是兩種不同的未達，處置也不同。

---

## 5. 下週預告（09/28 → 10/04）

- **M2 結算**：9/27 結果回寫 [ROADMAP §9.1](../roadmap/ROADMAP_OUTLINE.md)，B1 過或降級。
- **10/04 = C1／C2 切片**：C2 純函式若 A1 已提前交出，10/04 改排 `addPipeline` 內部防線。
- **StatsPanel 樣式**：S1 的 B 那半跨週續行（無死線）；`ItemSummaryTable` 的 props 必須保留（D1 十一月要接 `flowStore`）；三份重複字型抽共用。
- **goodmorning**：#48 落地後接佈局視角的設備方塊視覺（前提回到「工具列視覺進 master」＋「畫布已能落子」）。
- **MBD**：Storybook 上手確認後，再派不與人共檔的獨立 L3；方向見 [M0 §5](./MBD/0921/W0921-M0_pause.md)。
- **視角切換器**：仍未派，已連續三週順延。
- **不可用：** avery（約 10 月初回）、azure9572（11 月）。

---

## 6. 更新日誌

### 2026-09-24 — v1.2（追加裁示）

- **R-3′：** #48 本週必須修完；[G1](./goodmorning/0921/W0921-G1_toolbar_pr48_land.md) 升 v1.2；V3 恢復；合入序補回 G1（排在 T1 之後）
- **MBD：** 週報確認約 3–5h 有產能；**仍不派新功能碼**；[M0](./MBD/0921/W0921-M0_pause.md) 升 v1.2＋可選 [Storybook GUIDE](./MBD/0921/GUIDE_storybook_first_look.md)
- `ToolbarPanel.vue` 分區鎖補一句：G1 可動資料來源那幾行 script，不得刪落子意圖

### 2026-09-23 — v1.1（主編裁決回寫）

- §2.0 由「待裁清單」改為裁決紀錄，七項全列；**四項與 v1.0 的預設不同**
- **R-1 駁回／R-D1 撤銷：** 沒開 PR 的分支內容不算交付物。S1 §4、大綱 §6／§8／§9／§9.1、[D1 細項](../roadmap/detail/D1_stats_item_summary.md)同步改寫
- **R-2：** M1 作廢 → [M0 暫停單](./MBD/0921/W0921-M0_pause.md)（原檔刪除）；V7 取消
- **S1 擴大：** 搬家＋樣式，拆 A／B 兩支 PR，B 不下死線
- **R-3：** #48 維持現狀 → G1 全單改寫、T1 第 2 刀解除依賴、GUIDE §2 改寫、V3 取消、`ToolbarPanel` script 鎖即刻生效；合入硬條件記入 [B1 細項](../roadmap/detail/B1_toolbar_real_machines.md)
- **R-4 照准** → 大綱 §4／§9 的 R-B2 封鎖解除，狀態改 `[~]`
- **待審停滯 3 天點名不採納** → 從 §2.1／§3／§4 移除，R-E2 未新增
- §2.0 後記新增：MBD 與 goodmorning 本週無交付項，補派與否留給主編

### 2026-09-22 — v1.0

- 初版十一張工單。**延一日發工**，aaaaa 的 A0 交期因此由 9/23 改 9/24，toby 可接時間縮為 9/24–9/27
- §2.0：三項週會裁決未回傳，依決策層待確認清單的建議選項先發，另加 R-4（B2 解鎖範圍）
- §2.1 新增：落子意圖層不進 store、`label` 填中文名、`ToolbarPanel` 按區塊分鎖、「paper 過」改事後補審、S1 強制拆 PR
- §3 新增待審停滯 3 天點名規則；鎖表首次要求貼 Discord 並送達暫停者
- §4 為上週三個流程缺口的對應處置
