# R-A3 — 新人上手半頁

| meta | value |
|------|-------|
| 對應大綱 | [ROADMAP_OUTLINE.md](../ROADMAP_OUTLINE.md) §3 |
| 里程碑 | M1（2026-08-30） |
| 擋門檻 | 否（加分，但「新 clone 能開」由本項支撐） |
| 建議主責／備援 | dernoson／aaaaa |
| 性質 | 文件 |
| 依賴 | [A1](./A1_announce_and_baseline.md)（三條硬規則原文） |
| 狀態 | `[~]` Discord 置頂已生效；repo 半頁未貼入 |
| 最後更新 | 2026-08-30 |

---

## 1. 背景與動機

三層架構（L1／L2／L3）目前只存在於長篇文件裡，實務上常見的誤會有三種：以為 L2 是運算層、說不清楚三層各自負責什麼、不確定 Pinia 在哪一層。交檔方式也還沒有共識——用 GitHub 網頁上傳、把檔名當版本、把元件放進 `docs/<自己>/` 都發生過。既有的 `docs/dernoson/L1|L2|L3` 寫得完整，但**完整正是它不被讀完的原因**——對只有 3 小時的人來說，入口太深。

本項要的不是再一份教學，而是一張**半頁、能在會議上唸完、貼在 Discord 置頂**的紙。長版繼續存在，這張紙只負責把人送到正確的門口。

## 2. 使用者（協作者）看得到什麼

新成員拿到一張紙，照著跑得起 `pnpm dev`，知道自己要改的檔在三塊畫面的哪一塊，知道三件事絕對不能做。

## 3. 現況盤點

| 對象 | 現況 | 問題 |
|------|------|------|
| 根目錄 `README.md` | 有環境與指令 | 不談分層與畫面 |
| `docs/dernoson/L1|L2|L3` | 邊界與契約寫得很細 | 篇幅長，低工時者不會讀完 |
| `docs/toby/README.md` | toby 自寫的踩坑筆記 | 個人向，但證明「踩坑清單」有需求 |
| 半頁上手文件 | **不存在** | 本項要補 |

## 4. 技術決策

### 4.1 內容範圍（硬性半頁，超出即刪）

| 段 | 內容 | 行數上限 |
|----|------|----------|
| 1 | 跑起來：`pnpm install` → `pnpm dev` → 開哪個網址 | 4 |
| 2 | 三塊畫面：左上畫布、右側數字與警訊、下方選單；各自對應哪個資料夾 | 6 |
| 3 | 三條硬規則（[A1](./A1_announce_and_baseline.md) §4.1 原文） | 3 |
| 4 | 三件禁止事項（見 §4.2） | 3 |
| 5 | 卡住找誰：一張三列小表（環境／分層／資料） | 4 |

### 4.2 三件禁止事項（原文）

1. **禁止用 GitHub 網頁「Add files via upload」交檔**，一律走本機 commit ＋ PR。
2. **禁止用檔名當版本**（`XxxBtn.vue0811`、`Xxx_final.vue` 皆不收）。
3. **禁止把元件放到 repo 根目錄或 `docs/<自己>/`**，元件一律在 `src/components/<Name>/Index.vue` 或工單指定的既有路徑。

### 4.3 方案比較：放哪裡

| 方案 | 優點 | 缺點 | 採用 |
|------|------|------|------|
| A. 新開 `docs/ONBOARDING.md` | 顯眼 | 又多一個入口，與 dernoson 既有教學重疊 | 否 |
| **B. 放進 `docs/dernoson/` 既有教學開頭** | 不新增入口；主編本來就是守門人 | 需主編同意改自己的檔 | **是** |
| C. 只貼 Discord 置頂 | 零成本 | 無版本、三週後找不到 | 否（但同步貼一份） |

採 B 並同步貼 Discord 置頂；兩處內容以 repo 為準。

> 注意：`docs/dernoson/` 屬主編資料夾，aaaaa 不得直接修改。本項若由 aaaaa 代擬，須以草稿形式交付，由主編貼入。

### 4.4 三塊畫面對照（半頁用的簡表）

| 你看到的區塊 | 資料夾 | 你會改到的檔 |
|--------------|--------|--------------|
| 下方設備選單 | `src/editor/toolbar/` | `ToolbarPanel.vue` |
| 中央畫布 | `src/editor/canvas/`、`src/components/MachineShape.vue` | 由工單指定 |
| 右側數字與警訊 | `src/editor/stats/`、`src/components/StatsPanel/` | `Index.vue` 等既有檔 |

## 5. 檔案計畫

| 動作 | 檔案 | 說明 |
|------|------|------|
| 草稿 | 本檔 §4 各表 | aaaaa 代擬內容 |
| 貼入 | `docs/dernoson/` 既有教學檔開頭 | **由主編執行** |
| 同步 | Discord 置頂 | 內容一致 |
| 不新建 | — | 不開 `docs/ONBOARDING.md` |

## 6. 週切片

| 週日 | 切片 |
|------|------|
| 08/23 | 會上唸段 1–4；當場問「誰跑不起 `pnpm dev`」並記錄 |
| 08/30 | 依 8/23 實際卡點補段 5；跑不起來的人清零或列為已知阻塞 |

## 7. 不做

- 不寫 Vue 教學（週會段 3 只講本階段會碰到的詞彙）
- 不寫 Pinia 原理，只寫「L3 不 import store」這條結論
- 不做影片

## 8. 依賴與封鎖

依賴 [A1](./A1_announce_and_baseline.md) 的三條硬規則原文。若主編當週無工時貼入 `docs/dernoson/`，允許先以 Discord 置頂生效，repo 版順延至 8/30。

## 9. DoD

- [x] 內容不超過半頁（§4.1 行數上限內）（Discord 置頂版）
- [x] 三條硬規則與三件禁止事項以原文出現（隨 A1 公告／置頂）
- [ ] 已貼入 `docs/dernoson/` 既有教學（主編執行）——**8/30 仍未見獨立半頁檔；殘項**
- [x] Discord 置頂已同步（W0823-D0 DoD）
- [x] 8/23 會上確認過「誰跑得起 `pnpm dev`」，名單有紀錄（決策層／週會）

## 10. 風險與未交頂替

| 風險 | 對策 |
|------|------|
| 主編無工時寫長文 | Discord 先置頂生效；repo 版允許順延 |
| 半頁膨脹成教學 | §4.1 行數硬上限 |

**未交頂替：** 8/30 門檻不依賴本項；未完成則沿用口頭說明，順延至 9/6。**現況：置頂已生效，僅缺 repo 貼入。**

## 11. 開發日誌

### 2026-08-22
- 建檔

### 2026-08-23
- 隨 W0823-D0：Discord 置頂半頁內容生效；repo 貼入標「可 8/30 補」

### 2026-08-30
- **狀態 `[~]`。** 抽查 `docs/dernoson/` 無獨立上手半頁檔 → DoD「repo 貼入」仍未勾。9/6 前補完或明示沿用 Discord 置頂為準
