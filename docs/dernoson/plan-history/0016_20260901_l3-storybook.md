# 0016_20260901_l3-storybook

- **prev:** —
- **skill:** plan-history v3
- **status:** done

## 主題簡述

為 `src/components/` 底下的 L3 純展示元件建立 Storybook，讓每個元件能獨立掛載、窮舉 props 組合，不必把整條 app 跑起來才看得到某一種狀態。

現在做得起來，是因為 L3「不得 import store」這條硬規則已在全樹成立（唯一命中是 `BaseRegionSelector/Index.vue:51` 的型別引用，執行期會被抹除）。每個 L3 元件都能單獨掛載，story 不需要 Pinia decorator。

工具已定：Storybook ＋ `@storybook/vue3-vite`（見 O1）。

**本計畫的約束**

- 不為了讓 story 好寫而改 L3 元件的行為。story 寫不出來代表 props 介面有問題，回報給使用者，不改元件遷就 story。
- story 檔與元件同資料夾（`Index.stories.ts`），與 CLAUDE.md 第 2 節的元件資料夾慣例一致。
- `.storybook/` 要納入 ESLint 與 Prettier 的涵蓋範圍。

## 規劃描述

先裝套件與骨架，把 preview 的執行期前置（Tailwind、Nuxt UI plugin、Vue Flow CSS）補齊，再依外部依賴的難度分三批寫 story：無外部依賴的 6 個、走 Nuxt UI 的 1 個、需要 Vue Flow context 的 8 個。第一批同時是 preview 設定是否正確的驗證。

掛載位置（`/dev/storybook/<component>`）與 CI 涵蓋兩件事尚未定案，各自獨立成格。

## 觀察與推論

### O1 · 2026-09-01 03:05:00+08:00 — 候選套件只有 Storybook 相容於本專案的 Vite 8

`npm view` 實測：`@storybook/vue3-vite@10.5.10` 的 `peerDependencies.vite` 是 `^5 || ^6 || ^7 || ^8`，涵蓋專案的 Vite 8.0.10；`storybook@10.5.10` 的 peer 只有 `prettier`、`vite-plus`、`@types/react` 三項且全部 optional，不會拖進 React 依賴。`@storybook/addon-vitest@10.5.10` 的 peer 是 `vitest: ^3 || ^4`，與專案的 4.1.6 相容，但另外要求 `@vitest/browser` 與 `@vitest/browser-playwright`。

唯一的 Vue 原生對手 Histoire 停在 `1.0.0-beta.1`，`peerDependencies.vite` 是 `^7.3.0`，**不含 Vite 8**，且最後發布為 2026-01-07。要採用就得靠 `overrides` 硬壓 peer 範圍，把風險轉嫁到一個停滯的 beta 上。Ladle 只支援 React。

所以工具選擇沒有實質分歧：相容於現有 Vite 8 / Tailwind 4 / Vitest 4 的只有 Storybook 一條路。

### O2 · 2026-09-01 03:08:00+08:00 — L3 的 15 個元件依外部依賴分成三批

`src/components/` 下 15 個 `.vue`：8 個 import `@vue-flow/core`（`FlowChart/` 的 6 個節點與邊元件、`FlowChart/Index.vue`、`DeviceNode`），1 個用 Nuxt UI（`ShortcutRow/Index.vue` 的 `UButton` / `UKbd`），其餘 6 個只依賴 Vue 或無外部依賴。

沒有任何元件在執行期存取 Pinia。`BaseRegionSelector/Index.vue:51` 的 `import type { BaseRegion } from '@/store/canvasStore'` 是型別層對 store 層的反向依賴，不影響掛載，但值得記著。

Vue Flow 那 8 個靠 inject 取得畫布上下文，單獨掛載會失敗，需要把元件包進 `<VueFlow>` 的 decorator 並餵最小的 `nodes` / `edges`。這批的成本明顯高於其餘 7 個，所以要排在最後。

### O3 · 2026-09-01 03:12:00+08:00 — preview 要重現 main.ts 的四項執行期前置

`src/main.ts` 在掛載 app 前做了四件事：import `./style.css`、註冊 `@nuxt/ui/vue-plugin`、import `@vue-flow/core/dist/style.css` 與 `theme-default.css`。少任何一項，對應那批元件在 Storybook 裡就沒有樣式或直接掛不起來。

`vite.config.ts` 另有 `@nuxt/ui/vite` 的 `ui()` plugin 與 `@` alias。Storybook 的 vite builder 會讀專案的 `vite.config.ts` 並合併，理論上兩者都會生效——**但這一點尚未實測**，Nuxt UI 的 app config 與 color mode 在 Storybook 環境下是否完整仍是未知。

### O4 · 2026-09-01 03:30:00+08:00 — 元件計數更正：7 / 7 / 1，不是 8 / 6 / 1

- **更正:** O2

`src/components/` 底下 15 個 `.vue` 的實際分布是：`FlowChart/` 7 個（`Index`、`DeviceNode`、`MaterialNode`、`ProductNode`、`WarehouseNode`、`PendingImportNode`、`FlowEdge`）依賴 `@vue-flow/core`；`ShortcutRow/Index.vue` 1 個用 Nuxt UI；其餘 **7** 個（`BaseRegionSelector/Index`、`MachineShape`、`StatsPanel/` 的 5 個）只依賴 Vue 或無外部依賴。

O2 寫的「8 個 Vue Flow、其餘 6 個」是數錯，兩邊各差一。有機器後果：第 6 格的判準原本寫「8 個元件都能掛載」，會多要求一個不存在的元件。

### O5 · 2026-09-01 03:35:00+08:00 — Vite 設定確實被合併，但新增 CSS import 要重啟

Storybook 啟動時 log 出現 `[unplugin-vue-components] component "Index" ... has naming conflicts`，那是 `@nuxt/ui/vite` 的 `ui()` plugin 在跑——證實 builder 確實讀了專案根目錄的 `vite.config.ts` 並合併，`@` 別名與 postcss 也隨之生效。O3 標為未實測的那一點成立。

另一件事是操作上的：在 Storybook 已經跑著的情況下往 `preview.ts` 新增 CSS import，HMR 不會帶進來——瀏覽器端量到的是 `styleSheets` 只有 Storybook 自己的 3 份、`.p-4` 與 `.editor-layout` 都不存在，且 console 出現 `504 (Outdated Optimize Dep)`。重啟 dev server 後才變成 5 份、Tailwind 與 app 樣式都在、`<html>` 背景為 `rgb(9, 9, 11)`。改 preview 的 import 之後要重啟，不能只靠熱更新。

### O6 · 2026-09-01 03:48:00+08:00 — 六個 FlowChart 元件用了無單位的 Tailwind 任意值，尺寸實際失效

`FlowChart/Index.vue:116` 的外框寫 `h-[600]`，Tailwind 4 對無單位的任意值不產生 CSS，所以容器高度量到 `2px`（只剩上下框線），內部 `.vue-flow` 高度為 `0px`。五個節點確實有渲染進 DOM，但可視面積是零。同一個寫法還出現在 `DeviceNode.vue:17` 與 `PendingImportNode.vue:8` 的 `min-w-[15]`、`MaterialNode.vue:8`／`ProductNode.vue:8`／`WarehouseNode.vue:8` 的 `min-w-[12.5]`，共六處。正確寫法應帶單位（`h-[600px]`、`min-w-[15rem]` 之類）。

這個缺陷至今沒被發現，是因為 `FlowChart/Index.vue` 在 `src/` 內零引用——主編輯器與 `/dev` 都沒有掛它。把元件單獨掛起來才會照出來，這正是引入 Storybook 想要的效果。

依本計畫的約束不在此修：修的是 L3 元件的行為，不是 story 的問題，該由元件的維護者處理。

### O7 · 2026-09-01 03:50:00+08:00 — 節點與邊元件必須靠真正的畫布渲染

`FlowChart/` 的節點與邊元件吃的是 Vue Flow 的 `NodeProps` / `EdgeProps`：`selected` 由畫布的選取狀態給、`sourceX` / `targetY` 由畫布依兩端節點位置算出。因此 story 不能直接掛元件並手動餵這些欄位，只能把元件註冊成 `nodeTypes` / `edgeTypes`，交給真正的 `<VueFlow>` 渲染。

連帶兩個後果：一是 meta 不能設 `component`，否則 Storybook 會要求 args 滿足整個 `NodeProps`（30 餘個欄位），props 表也只會列出 Vue Flow 的介面而非元件自身的契約；改以 `argTypes` 與 docs description 描述 `data` 的形狀。二是 `h(VueFlow, ...)` 的 props 與 emits 型別無法對上（`onError` 等事件的簽章推導不相容），harness 內以 `Component` 收窄檢查範圍並就地註明理由。

## 待辦

### 1 套件安裝與 Storybook 骨架

- **state:** 完成
- **basis:** → O1

安裝 `storybook`、`@storybook/vue3-vite`、`@storybook/addon-docs`、`@storybook/addon-a11y` 四個 devDependency，建立 `.storybook/main.ts` 與 `preview.ts`，並在 `package.json` 加 `storybook` 與 `build-storybook` 兩個 script。

`@storybook/vue3-vite` 的 dependencies 已含 `vue-docgen-api` 與 `vue-component-meta`，所以 `defineProps` / `defineEmits` 的逐欄位 JSDoc（CLAUDE.md 第 3 節已強制要求）會自動產生 props 表——這是既有註解規範的直接回收，不需要為 Storybook 另寫一套說明。

`addon-a11y` 值得加：L3 是純展示層，可及性問題只有在這一層抓得到。

Storybook 以自己的埠與原生 URL 提供，不設 base path、不掛進 app 路由（見第 3 格）。

判準：`pnpm storybook` 起得來，且 `.storybook/` 通過 lint 與 format。

**沿革**

- H1 · 2026-09-01 落地 —— Storybook 10.5.10 與三個 addon 裝上，`.storybook/` 納入 Prettier 與 type-check，`storybook-static` 加入 ESLint ignores 與 `.gitignore`

### 2 preview 的執行期前置

- **state:** 完成
- **needs:** 1
- **basis:** → O3、O5

`.storybook/preview.ts` 要重現 `main.ts` 的四項前置：`src/style.css`、`@nuxt/ui/vue-plugin` 的 `setup()` 註冊、`@vue-flow/core` 的兩份 CSS。`@` alias 與 `postcss.config.js` 由 Vite 沿用，不需另設。

`ui()` plugin 確實隨專案 vite config 生效（O5）。Pinia 不註冊：L3 不得 import store，註冊了反而會讓違規的元件看起來能動。

判準：第 4 格的元件在 Storybook 裡樣式與主編輯器一致。

**沿革**

- H1 · 2026-09-01 落地 —— preview 補上 `src/style.css` 與 Vue Flow 兩份樣式、註冊 Nuxt UI plugin；重啟後量到 Tailwind 與 app 樣式皆生效 → O5

### 3 Storybook 掛在 /dev/storybook/<component> 的方式

- **state:** 否決
- **basis:** → O1

Storybook 以自己的埠與原生 URL 提供（管理介面 `/?path=/story/<title>--<name>`、單一 story `/iframe.html?id=<id>`），**不掛進 app 路由、不設 base path**。`src/router/index.ts` 與 `/dev` 既有頁面都不動。

原本設想的 `/dev/storybook/<component>` 需要在 app 路由裡包一層 iframe 並維護 component 到 story id 的對照，且 Storybook 必須先 build 或並行跑著。相對於「多記一個埠」，這個代價不划算。

story 的入口因此是 `pnpm storybook`，見第 1 格。

**沿革**

- H1 · 2026-09-01 決斷 —— 自 `/dev/storybook/<component>` 的原始設想開格，列出四個方向待裁（使用者）
- H2 · 2026-09-01 否決 —— 採 Storybook 原生 URL，不掛進 app 路由（使用者）



### 4 無外部依賴元件的 story

- **state:** 完成
- **needs:** 2
- **basis:** → O4

為只依賴 Vue 或無外部依賴的 7 個元件建 story：`BaseRegionSelector/Index.vue`、`MachineShape.vue`、`StatsPanel/` 的 `Index`、`ItemSummaryTable`、`PowerSummary`、`TicketEstimate`、`WarehouseEstimate`，共 16 個 story。

這一批同時是第 2 格的驗證：Tailwind 與 preview 設定若有問題，在這裡就會顯現，不必等到 Vue Flow 那批。

`StatsPanel/Index` 不吃 props，因此只有一個 story——沒有可變輸入就沒有邊界情形可列，各子區塊的邊界在它們自己的 story 裡。`MachineShape` 的非方形案例以「第一台寬高不等的機器」取得而非寫死名稱，資料調整時仍指向真正的非方形機。

判準：每個元件至少一個預設 story 與一個邊界 story（空資料 / 極端值），且 props 表由 JSDoc 自動產生。

**沿革**

- H1 · 2026-09-01 落地 —— 16 個 story 全數渲染，props 表帶出元件既有的繁中 JSDoc；a11y 面板無 violation → O4、O5

### 5 ShortcutRow 的 Nuxt UI 路徑

- **state:** 完成
- **needs:** 2
- **basis:** → O4、O5

`ShortcutRow/Index.vue` 是唯一用到 Nuxt UI（`UButton` / `UKbd`）的 L3 元件，單獨成格是因為它驗證的是另一條路徑：Nuxt UI 的元件解析與樣式在 Storybook 環境下是否完整。

判準：`UButton` 與 `UKbd` 在 story 中的外觀與主編輯器一致。

**沿革**

- H1 · 2026-09-01 落地 —— 五個 story（一般鍵位、衝突、錄製中、單鍵、未綁定）全數渲染，`UKbd` 徽章與 `UButton` 樣式正常，a11y 無 violation → O5

### 6 Vue Flow 元件的 decorator 與 story

- **state:** 完成
- **needs:** 2
- **basis:** → O4、O6、O7

`FlowChart/` 的 7 個元件靠畫布提供上下文與計算後的 props（O7）。共用工具放在 `.storybook/vueFlowHarness.ts`：`nodeHarness` 把元件註冊成 `nodeTypes`、`edgeHarness` 註冊成 `edgeTypes` 並額外放兩個端點節點，兩者都關掉拖曳與縮放——story 要看的是元件外觀，不是畫布互動。工具放 `.storybook/` 而非元件資料夾，因為它是 Storybook 的基礎設施，不是元件程式碼。

`FlowChart/Index.vue` 自帶 `<VueFlow>` 與寫死的示範資料，不需 harness 也只有一個 story。

七個元件共 23 個 story，全數渲染、console 無 inject 警告、a11y 無 violation。過程中照出 O6 的尺寸缺陷，依約束不在此修。

判準：7 個元件都能掛載且不噴 inject 警告。

**沿革**

- H1 · 2026-09-01 落地 —— harness 與七個元件的 story 完成；`h(VueFlow)` 的型別問題與 meta 不設 `component` 的取捨記於 O7 → O7
- H2 · 2026-09-01 修正 —— 掛載過程照出六處無單位的 Tailwind 任意值，記為 O6 交回使用者，不在本格修

### 7 story 的 CI 涵蓋與互動測試

- **state:** 否決
- **basis:** → O1

story 不納入 CI，也不裝 `@storybook/addon-vitest` 與 Playwright。`validate-all` 維持四步不變，`build-storybook` 不進 CI。

理由是專案目前沒有規範 story 品質的規章：沒有定義一個元件該有哪些 story、story 壞掉算不算擋合入、誰負責維護。在這些沒定之前把 story 綁進 CI，只會產生一道沒人知道該怎麼修的紅燈。

要重啟這件事，先立規章再開新格，不在本格續做。

**沿革**

- H1 · 2026-09-01 決斷 —— 自套件相容性盤點開格，CI 涵蓋與互動測試待裁（使用者）→ O1
- H2 · 2026-09-01 否決 —— 尚無規範 story 品質的規章，不納入 CI、不裝 addon-vitest（使用者）
