# Agent Working Rules

本文件只規範 Agent 的工作行為。專案需求與架構以現有程式碼、
任務文件及使用者當次指示為準。不得從其他專案推導或移植架構。

## 1. Scope

- 只修改完成當前任務所必需的檔案。
- 不得順手重構、整理、重新命名或改寫無關程式碼。
- 不得自行建立中介層、adapter、registry、wrapper、fallback、
  compatibility layer 或新的抽象。
- 不得因為「架構更乾淨」而擴大修改範圍。
- 若修改將跨越未被指示的模組，必須先停止並向使用者確認。

## 2. Unknowns

- 不得猜測不存在或尚未確認的 API、型別、資料格式與產品需求。
- 找不到依據時，先搜尋現有程式碼、型別、測試及文件。
- 搜尋後仍無法確定，必須明確列出 unknown 並詢問使用者。
- 不得用 fallback、預設值、型別斷言或相容程式碼掩蓋 unknown。

## 3. Existing Architecture

- 優先沿用專案目前已有的型別、函式與資料流。
- 不得從其他專案搬入架構、命名或設計模式。
- 修改前先讀取目標檔案、直接依賴者及相關型別。
- 除非任務明確要求，不得更改公開 API、輸出格式或既有行為。
- 發現既有設計可能有問題時，只需報告；不得自動重構。

## 4. Minimal Diff

- 採用能完成需求的最小修改。
- 不得進行大範圍文字取代或不受控的跨檔案 rename。
- 不得修改與任務無關的 formatting、import 排序或註解。
- 若實際 diff 明顯大於預期，必須停止並說明原因。

## 5. Validation

- 不得宣稱未實際驗證的內容已通過。
- 必須清楚區分：
  - 已由程式碼確認；
  - 已由測試確認；
  - 僅為推測；
  - 尚未驗證。
- 執行測試或編譯前，先確認其範圍與成本。
- 測試失敗時，不得擅自修改其他模組來讓測試通過。

## 6. Completion

完成後只回報：

1. 修改了哪些檔案；
2. 各修改解決什麼需求；
3. 執行了哪些驗證及結果；
4. 尚存的 unknown、風險或未驗證項目。

不得把額外建議直接實作。